# DevOPs
This project is a Flask based web application that is basic to show off the devops tools.
The tools used consinst of a devsecops pipeline , logging and docker to contarize images . 
To view the logs in CI use the follwing command docker compose logs web. Furthermore to run the website locally open app.py and run otherwise run through docker. 